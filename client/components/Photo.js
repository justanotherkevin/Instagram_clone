import React from 'React';

const Photo = React.createClass({
    render() {
        return (
            <figure clasName="grid-figure">
                I am a photo!
            </figure>
        )
    }
})

export default Photo;
